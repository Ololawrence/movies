import React from 'react';
import './PageNotFound.scss';

const PageNotFound = () => {
    return (
        <div className = 'wrapper'>
           page not found 
        </div>
    );
};

export default PageNotFound;